version: '3.8'

services:
  app:
    build: .
    ports:
      - "8001:8001"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./app:/app/app
      - ./project.db:/app/project.db
    profiles: ["dev"]
